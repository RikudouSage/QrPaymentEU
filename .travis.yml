language: php
php:
  - '7.1'
  - '7.2'
  - '7.3'
  - nightly
install: 'composer install'
after_script:
  - '[ -f "./tmp/skip_coverage" ] || ./vendor/bin/php-coveralls --verbose ; touch ./tmp/skip_coverage'
branches:
  only:
    - master
